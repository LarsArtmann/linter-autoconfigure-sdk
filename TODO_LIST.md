# TODO List

Short- and mid-term actionable work. Open items only — completed items live in
`CHANGELOG.md`. Long-term ideas and open questions live in `ROADMAP.md`.

Harvested from `docs/status/2026-09-09_01-54_public-flip-mit-relicense.md`
(section f) plus still-open, code-verified items from the 2026-07 status
reports. Every item was checked against the code before landing here.

| ID  | Task                                                                                                                                                                                                    | Impact | Effort | Evidence / Source                                                       |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | ------ | ----------------------------------------------------------------------- |
| T1  | Verify the module is fetchable externally: `go get github.com/larsartmann/linter-autoconfigure-sdk` from a clean throwaway module                                                                       | High   | S      | flip report f3; repo public since `23e74f1`, proxy fetch never verified |
| T2  | Trigger the pkg.go.dev listing (proxy fetch) and verify rendering: godoc, `Example*` functions, license; re-check after 24h for propagation lag                                                         | High   | S      | flip report f4, f43; zero tags → pseudo-versions only                   |
| T3  | Add CI: `.github/workflows/ci.yml` running `buildflow --fix --fail-on-findings` on push/PR (needs `GOEXPERIMENT=jsonv2` env)                                                                            | High   | M      | flip report f6/c4; no `.github/` dir exists                             |
| T4  | Decide + cut first tag `v0.1.0` and create the GitHub Release with notes (needs owner go-ahead on API freeze; see ROADMAP Q2)                                                                           | High   | S      | flip report f7/f8; `git tag` is empty                                   |
| T5  | Fix unchecked `os.RemoveAll` returns in `example_test.go:17` and `:33` (errcheck findings)                                                                                                              | Medium | S      | LSP errcheck diagnostics; jsontext report item 1                        |
| T6  | Add a sentinel test + doc example for `ErrNoRepair` (exported, currently untested and unused in-repo)                                                                                                   | Medium | S      | `autoconfigure.go:239`; 07-19 report items 16-17                        |
| T7  | Add regression tests for `(*ConfigError).As` / `.Is` delegation to the wrapped cause (only `errors.AsType` is asserted today)                                                                           | Medium | S      | `autoconfigure.go:76-78`; jsontext report item 8                        |
| T8  | Rewrite `TestSaveJSON_Idempotent_NoRewriteOnSameContent` to be filesystem-independent (inode/content-hash, not the 20ms-sleep mtime comparison that can false-pass on coarse-mtime filesystems)         | Medium | S      | `autoconfigure_test.go:194-227`; write-if-changed report d.3            |
| T9  | Add concurrency test: parallel `SaveJSON` calls to the same path surface `*ConfigError` wrapping `atomicwrite.ErrConcurrentModification`; plus a `WriteIfChanged` write-error-path test (read-only dir) | Medium | M      | `autoconfigure.go:130`; write-if-changed report f3/f22                  |
| T10 | Add "Requires `GOEXPERIMENT=jsonv2` in Go 1.26 (GA in Go 1.27)" to the package doc comment in `autoconfigure.go:1-17`                                                                                   | Medium | S      | jsontext report item 9; doc currently silent                            |
| T11 | Clear `go-structure-linter` findings: add a minimal `.golangci.yml` and document why the flat layout (no `internal/`, no `examples/` dir) is intentional                                                | Medium | S      | jsontext report items 4-6; no `.golangci.yml` tracked                   |
| T12 | Add `SECURITY.md` with a security contact                                                                                                                                                               | Medium | S      | flip report f14; file absent                                            |
| T13 | Enable branch protection on `master` (public default branch, auto-commit daemon pushes to it)                                                                                                           | Medium | S      | flip report f15                                                         |
| T14 | GitHub polish: set repo topics (`go`, `linter`, `sdk`, `golangci-lint`, `oxlint`, `config`), refresh description with install command, add `dependabot.yml`                                             | Medium | S      | flip report f19-21                                                      |
| T15 | Add issue + PR templates, `CODEOWNERS`, and a social preview image                                                                                                                                      | Low    | S      | flip report f30-32                                                      |
| T16 | Decide `SaveJSON`'s discarded `changed` bool: keep as-is, change signature, or add `SaveJSONIfChanged` variant (zero consumers → free to change now; see ROADMAP)                                       | Medium | S      | `autoconfigure.go:130`; write-if-changed report Q2                      |
| T17 | Sweep tracked `docs/` for internal project names (`licenseforge`, BuildFlow internals) and redact or accept each                                                                                        | Medium | S      | flip report f18                                                         |
| T18 | Audit README as the public sales page: verify the install snippet end-to-end from a clean machine, tell the why-it-exists story without relying on two private repos (see ROADMAP Q3)                   | Medium | M      | flip report f12/f33                                                     |
| T19 | Publish coverage (buildflow already writes `reports/coverage.out`) as a CI artifact or badge                                                                                                            | Low    | M      | flip report f44                                                         |
