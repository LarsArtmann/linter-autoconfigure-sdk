# go-finding (latest) imports encoding/json/v2, which is gated behind
# the goexperiment.jsonv2 build tag in Go 1.26.x. direnv loads this
# automatically on cd into the repo. See AGENTS.md for full rationale.
export GOEXPERIMENT=jsonv2
